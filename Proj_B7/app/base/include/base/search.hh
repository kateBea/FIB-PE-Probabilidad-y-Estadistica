
#ifndef SEARCH_HH
#define SEARCH_HH

#include <base/gentree.hh>

namespace base {

    //auto exists_bfs(const gentree& tree, const node& n) -> void;
    //auto exists_dfs(const gentree& tree, const node& n) -> void;

    //auto search_bfs(const gentree& tree, const node& startpoint, const node& endpoint) -> bool;
    //auto search_dfs(const gentree& tree, const node& tartpoint, const node& endpoint) -> bool;
}

#endif //SEARCH_HH